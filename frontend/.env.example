# === API & Sockets ===
# IMPORTANT: API_BASE_URL should include the /api prefix because the axios client
# uses it directly (e.g., POST /auth/login → https://api.example.com/api/auth/login).
API_BASE_URL=https://api.example.com/api
SOCKET_BASE_URL=https://api.example.com
# If your server uses a custom Socket.IO path (backend SOCKET_PATH), set it here too.
SOCKET_PATH=/socket.io

# === 3rd-party / features (optional) ===
# Weather screen (used by frontend/utils/weather.js via @env)
WEATHER_API_KEY=replace-with-openweather-key
# Only used if your constants pull from env (recommended):
WEATHER_DEFAULT_CITY=Austin
WEATHER_UNITS=imperial

# Live support chat deep-link (used by LiveChatSupport/Referral screens if configured)
SUPPORT_CHAT_URL=https://support.example.com/chat

# === Build-time notes ===
# This project uses `@env` import for runtime variables in React Native.
# Ensure your bundler/babel plugin is configured (e.g., react-native-dotenv).
# If you use Expo and prefer Expo’s public envs, you may also mirror keys as:
# EXPO_PUBLIC_API_BASE_URL, EXPO_PUBLIC_SOCKET_BASE_URL, etc. (not required for @env).
