# === Core server ===
NODE_ENV=production
PORT=3000

# === Database ===
# Use either MONGO_URI or MONGODB_URI; server reads MONGO_URI || MONGODB_URI
MONGO_URI=mongodb+srv://user:pass@cluster.example.mongodb.net/prod
# MONGODB_URI=mongodb://localhost:27017/app

# === CORS / Security ===
# Comma-separated list of allowed origins. If empty, ALL origins are allowed (dev).
# Example: https://app.example.com,https://admin.example.com
CORS_ORIGIN=https://app.example.com

# Enforce the Socket.IO path if you changed it (the server reads SOCKET_PATH or legacy SOCKET_NAMESPACE_PREFIX)
SOCKET_PATH=/socket.io
# SOCKET_NAMESPACE_PREFIX=/socket.io

# Optional: Redis for Socket.IO horizontal scaling. Leave blank to disable.
# Format examples:
# redis://localhost:6379
# redis://:password@redis-host:6379
REDIS_URL=

# === JWT / Auth (match your auth route implementation) ===
# If your auth code uses separate secrets for access/refresh tokens:
JWT_ACCESS_SECRET=replace-with-strong-random-value
JWT_REFRESH_SECRET=replace-with-strong-random-value
# If your code uses a single secret, also set:
JWT_SECRET=replace-with-strong-random-value

# === Email / SMS providers for 2FA (pick what your code uses) ===
# SMTP (e.g., Nodemailer)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=apikey-or-username
SMTP_PASS=replace-with-secret
SMTP_FROM="SwiftCampus Support <no-reply@example.com>"

# Twilio (if SMS 2FA)
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_FROM_NUMBER=

# === File storage for profile image review (if backend uploads images) ===
# Leave blank if you only accept public URLs from the app.
S3_BUCKET=
S3_REGION=
S3_ACCESS_KEY_ID=
S3_SECRET_ACCESS_KEY=
S3_PUBLIC_BASE_URL=https://your-cdn.example.com

# === Maps / Geocoding (if routes/maps use third-party services) ===
GOOGLE_MAPS_API_KEY=

# === Logging ===
# LOG_LEVEL can be used by your logger util if implemented (info, warn, error, debug)
LOG_LEVEL=info
