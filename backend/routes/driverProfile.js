const express = require('express');
const router = express.Router();
// TODO: driver profile logic
router.get('/', (req, res) => res.json({ message: 'Driver profile endpoint placeholder' }));
module.exports = router;
