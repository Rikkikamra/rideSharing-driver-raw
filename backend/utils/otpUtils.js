// Stub for sendOTP(email, phone, otp)
// Replace with SendGrid/Twilio integration
module.exports.sendOTP = async (email, phone, otp) => {
  console.log(`Sending OTP ${otp} to ${email} and ${phone}`);
};